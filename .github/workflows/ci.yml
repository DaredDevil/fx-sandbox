name: ci

# This workflow is generated by Agentic's init-claude-config.ps1.
# All actual CI logic lives in the reusable workflow at:
#   elveedeveloper/agentic/.github/workflows/reusable-ci-dotnet.yml@main
# Re-run init-claude-config.ps1 -Force to regenerate this file when the
# pattern changes in Agentic.

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  check:
    uses: elveedeveloper/agentic/.github/workflows/reusable-ci-dotnet.yml@main
    with:
      dotnet-version: '8.0.x'
