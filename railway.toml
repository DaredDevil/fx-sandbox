[build]
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "dotnet FxSandbox.Api.dll"
healthcheckPath = "/api/rates"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
